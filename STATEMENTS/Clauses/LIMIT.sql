SELECT *
FROM table1
LIMIT n OFFSET m; -- n: INT >= 0, M: INT >= 0

/*
LIMIT es una CLAUSE que se complementa con OFFSET, esta clause establece un limite de filas que retornara la QUERY, OFFSET se salta la cantidad deseada de filas.
*/

