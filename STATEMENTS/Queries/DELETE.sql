DELETE 
FROM table1

/*
DELETE es una QUERY mediante la cual se pueden borrar filas de una tabla.
Se puede filtrar mediante el uso de JOIN, WHERE Y ORDER BY + LIMIT.
No se puede utilizar clauses distintas a las mencionadas anteriormente de manera directa, se pueden usar en subqueries.
Si no se especifica nada, se borran todas las filas de la tabla.
*/
