SELECT * 
FROM table1;

/* 
SELECT es una QUERY (consulta) que filtra las columnas de una tabla, se le pueden colocar parametros como WHERE, GROUP BY, ORDER BY etc 
con los que se puede aumentar la complejidad de las consultas. 
*/