SELECT column1
FROM table1
WHERE column1 BETWEEN value1 AND value2;

/*
BETWEEN es un OPERATOR que devuelve filas con columnas que caen en un rango espefico de valores, sirve para valores numericos y cadenas de caracteres.
BETWEEN se comporta muy raro con str, mejor utilizar alternativas.
*/